# _xmlcatalog_Controller : _CLI_Controller

`_xmlcatalog_Controller` is an example subclass of `_CLI_Controller` for the `xmlcatalog` class without a UI. 

The response is stored in `This.instance.data`.
